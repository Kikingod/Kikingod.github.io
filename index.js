console.log('Before pause');

// Using setTimeout to create a delay of 1000 milliseconds (1 second)
setTimeout(function() {
  console.log('After one second pause');
  // The code inside this function will be executed after the delay
}, 1000);

console.log('After setTimeout, but before pause');
